# Sublime Text config files

Save all of these files, except for `pl.sublime-project`, to:

```
~/Library/Application Support/Sublime Text/Packages/User/
```

`pl.sublime-project` is an example of a project config and lives in the root of your codebase.
